<template>
    <view class="loading">
        <text :style="{ color: c.textSecondary, fontSize: fontPx(FONT_XS) }">{{ t('common.loading') }}</text>
    </view>
</template>

<script setup lang="uts">
import { colors, AppColors } from "../../common/theme.uts"
import { fontPx, FONT_XS } from "../../common/layoutScale.uts"
import { t } from "../../i18n/i18n.uts"

defineOptions({
    name: "LoadingView"
})

const c = computed<AppColors>((): AppColors => colors())
</script>

<style scoped>
.loading {
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: center;
    padding: 10px;
}
</style>
