<template>
    <view class="conference-invite-message-container" @tap="showConferenceInfo"
        :class="{ out: message.direction == 0 }" :style="{ backgroundColor: message.direction == 0 ? c.bubbleSent : c.bubbleReceived }">
        <view class="flex-row flex-align-center">
            <image class="avatar" :src="portrait" :style="{ backgroundColor: c.inputBg }" />
            <view class="flex-1">
                <text class="single-line title" :style="{ color: message.direction == 0 ? c.bubbleSentText : c.bubbleReceivedText }">{{ content.title }} </text>
                <text class="single-line desc" :style="{ color: c.textSecondary }">{{ content.desc }}</text>
            </view>
        </view>
        <text class="type" :style="{ color: c.textSecondary, borderTopColor: c.hairlineSoft }">会议邀请</text>
    </view>
</template>

<script setup lang="uts">
import Message from "@/wfc/messages/message.uts";
import ConferenceInviteMessageContent from "@/wfc/av/messages/conferenceInviteMessageContent.uts";
import store from "@/store.uts";
import ConversationType from "@/wfc/model/conversationType.uts";

import { colors, AppColors } from "@/common/theme.uts";
defineOptions({
    name: "ConferenceInviteMessageContentView",
    styleIsolation: 'app'
})


const c = computed<AppColors>((): AppColors => colors())
const props = defineProps<{
    message: Message
}>()

const content = computed<ConferenceInviteMessageContent>((): ConferenceInviteMessageContent => {
    return props.message.messageContent as ConferenceInviteMessageContent
})

const portrait = computed<string>((): string => {
    const groupId = props.message.conversation.type == ConversationType.Group ? props.message.conversation.target : '';
    const userInfos = store.getUserInfos([content.value.host], groupId)
    return userInfos.length > 0 ? userInfos[0].portrait : '';
})

const showConferenceInfo = () => {
    // uni-app x 版本暂不支持音视频会议
    uni.showToast({
        title: '当前版本不支持会议功能',
        icon: 'none',
    } as ShowToastOptions);
}
</script>

<style scoped>
.conference-invite-message-container {
    margin: 0 10px;
    padding: 5px;
    width: 250px;
    position: relative;
    border-radius: 5px;
}

.avatar {
    width: 60px;
    height: 60px;
    border-radius: 3px;
    margin: 5px 10px;
}

.type {
    padding-top: 5px;
    margin: 5px 10px 0 10px;
    border-top-width: 1px;
    border-top-style: solid;
    font-size: 14px;
}

.title {
    font-size: 16px;
}

.desc {
    font-size: 14px;
}
</style>
